# カミワザ アーキテクチャ — 設計判断の記録

README が「何があるか」を書く場所だとすると、ここは「なぜその形にしたか」を書く場所です。書いているのは私(ソロ参加)。各節に実装ファイルのパスを添えてあるので、主張とコードを突き合わせながら読んでください。数値は、全対外主張に分母・経路・測定日を紐づけて管理している内部の検証記録の値だけを使います。記録自体は公開物に含めていないため、この文書では数値ごとに限定句を直接添え、2026-08-11 に測ったばかりの実測値には日付を注記します。

残っている限界・未実装は別文書 [KNOWN_ISSUES.md](KNOWN_ISSUES.md) に先に書き出してあります。良い話だけ読みたくない人はそちらからどうぞ。

## 全体像

```
紙の写真(iPad / スマホ / PC ブラウザ)
   │  POST /api/analyze(JSONボディ12MB上限 = app/src/lib/http.ts)
   ▼
┌─ ライブ経路(APIキーあり)────────────────────────────────┐
│ 向き検出: tool_use 強制 ×1〜2回(claude-live.ts / rotation.ts)│
│   └ 非ゼロ判定のみ二重確認。合議一致時だけ sharp.rotate()     │
│ 本解析: Claude Vision SSE ストリーミング                      │
│   ├ [層1] フェンス耐性 部分JSONパーサ(partial-json.ts)       │
│   ├ [層2] アプリ側スキーマ検証+意味検査(validate-spec.ts)    │
│   └ 検証違反・例外は throw ──────────┐                        │
└──────────────────────────────────────┼────────────────────────┘
                                       ▼ 自動フォールバック(画面に明示)
┌─ デモ経路(キー不要・LLM非経由)──────────────────────────┐
│ scenarios.ts のシナリオから demo.ts がイベント列を決定論生成   │
└────────────────────────────────────────────────────────────────┘
   │  どちらも同一の AnalyzeEvent 列(events.ts)
   ▼
クライアント(KamiwazaApp.tsx)
   ├ ストリームを逐次描画
   └ [層3] done.spec との照合・無条件再構築(reconcile.ts)
   ▼
決定論レンダラー(SpecApp.tsx)= AppSpec だけを描画する
```

この図で言いたいことは3つです。ライブとデモが同じイベント列に合流すること。ライブ経路の防御が3層あって、どの層で失敗してもデモに落ち、画面が止まらないこと。そしてその切替は画面に明示され、以降のデータが撮影した紙由来でないことを操作者が誤認しないこと(§2)。

## 1. なぜ LLM に生コードを書かせないのか

「AIがアプリを作る」と聞いてまず思い付くのはコード生成ですが、採りませんでした。生成されたコードは検証も再現もできず、未知の紙で壊れたときに現場で止まるからです。

代わりに、モデルの出力を AppSpec DSL(`app/src/lib/appspec.ts`)に閉じました。全階層 `additionalProperties: false` の JSON Schema(`ANALYZE_OUTPUT_JSON_SCHEMA`)でスキーマ外のキーを封じ、描画はその spec だけを入力とする決定論レンダラー(`app/src/components/SpecApp.tsx`)が行います。モデルが出せるのは文字列・数値・enum だけで、それがコードや HTML として解釈される場所はアプリ側にありません。

生成後の「日本語で書いて直す」も同じ発想です。モデルに spec 全体を書き直させるのではなく、閉じた6操作(`app/src/lib/specdiff.ts` の `SpecDiff`)だけを tool use で発行させます。削除・実行・外部呼び出しに相当する操作は定義自体が存在せず、操作対象の ID は現行 spec の実 ID をツールスキーマの enum に動的注入するため、存在しない項目は指定できません。適用は純粋関数 `applyDiff` で、未知の op・null・undefined・非オブジェクトのいずれでも元の spec が同一参照で返ります。

「閉じている」ことは運用規約ではなく、コンパイルとテストの両方で固定しています。reducer の網羅 switch は default 節で `never` 代入を行うため、7種目の op を足すと型検査が落ちます。スキーマ側はテストが `ANALYZE_OUTPUT_JSON_SCHEMA` の全オブジェクトノードを再帰走査して `additionalProperties: false` の抜けを検出し(`appspec.test.ts`)、enum 注入側は「enum に載った ID は必ず applyDiff に受理される」ことを全シナリオでプローブします(`specdiff.test.ts`)。

「モデル出力がコードとして解釈される場所を作らない」の唯一の例外は、アプリの外にあります。CSV書き出しを Excel で開く経路です。ラベルも値も紙をモデルが読んだ結果なので、紙に `=HYPERLINK(...)` と書いておくだけで数式として評価されえます。先頭が `=` `+` `-` `@` 等のセルは `'` を付けて無害化し、数値リテラルだけは素通しにしました(金額が文字列化すると業務側の集計が壊れるため)。全5シナリオの書き出しがこの対策の前後でバイト単位一致することを確認済みです(`SpecApp.tsx` の `csvEsc`)。

副産物として、ワイヤ形式は歪んでいます。`firstRecord` がペア配列、明細行が列順の文字列配列(`appspec.ts` の `AnalyzeOutput`)なのは、`additionalProperties: false` を全階層に要求すると動的キーが使えないためです。「きれいに Record 型へ」というリファクタは API に拒否されます。歪みは `toAppSpec()` の一箇所で吸収しています。

## 2. デモとライブが同じイベント列を流す理由

`app/src/lib/events.ts` の `AnalyzeEvent` が唯一のプロトコルです。ライブ解析(`claude-live.ts`)もデモ再生(`demo.ts` + `scenarios.ts`)も同じ列を流すので、クライアントは接続先がどちらかを意識しません。

この対称性は見た目の統一のためではなく、フォールバックの土台です。ライブ解析が途中で失敗したとき、サーバーは同じ接続の上でデモ列を流し直すだけでよく、クライアント側に「ライブ用の失敗処理」という別コードパスが生まれません。後述する done.spec 照合復元も、デモ経路では構成上そのまま成立します(demo.ts は spec からイベントを生成するため、ストリームと spec は恒等)。

フォールバックは静かにやりません。切替時に「ライブ解析に失敗したため、デモデータで続行します」というイベントを同一ストリームで流し(`app/src/app/api/analyze/route.ts`)、画面の生成ログに出します。自分の紙を撮った人の画面に無関係なデモデータが出るとき、それをデモだと明示しないのはデータ完全性の問題だからです。

デモ経路は LLM 非経由であることを、イベントの形でも守っています。usage・costUsd などのキーはライブ経路でのみ付与し、デモでは 0 を流すのではなくキー自体を流しません。実測していない数字を画面に出さないためです。

ブラウザなしで確認する場合はこれで足ります(キー不要):

```bash
curl -N -X POST localhost:3000/api/analyze \
  -H 'Content-Type: application/json' -d '{"scenarioId":"chumonsho"}'
```

## 3. 信頼性の3層 — 各層が何を拾い、何を拾えないか

ライブ経路の防御は、受信順に3層あります。

**層1: フェンス耐性の部分JSONパーサ**(`app/src/lib/partial-json.ts`)。OrcaRouter 経由では応答が ```json フェンス付きで返るため(理由は §6)、フェンス・前置きの散文・delta 境界で分割された閉じフェンスを除去してからパースします。ストリーミング途中の未完成 JSON は閉じ括弧を補って逐次描画に使い、失敗したら次のトークンを待つだけです。転送層の重複で末尾の構造文字が二重適用されたバッファには、括弧の対応が取れた先頭の JSON を取り出す第2救済(`extractBalancedJson`)を用意しました。これが無いと、last-comma 救済が fields ごと切り詰めた別物を「成功」として返します(破壊テストで発見した退行。修正済み)。

**層2: アプリ側スキーマ検証+意味検査**(`app/src/lib/validate-spec.ts`)。最終 JSON に全階層 `additionalProperties: false` の構造検査をかけ、さらに「スキーマには適合するが fields 0件・appName 空白」という成立しない spec を意味検査(`validateSpecMinimums`)で失格させます。違反時は throw して既存のデモフォールバック連鎖へ流すだけで、新しい失敗モードは作りません。検証結果は SSE の `validation` イベントで画面にも出します。バリデータは ajv 非依存の最小実装で、未対応のスキーマキーワードに出会ったら黙って通さず throw します——検査の抜けをザルではなくエラーにする構えです。なお `minItems`/`minLength` をワイヤ側スキーマに書かずアプリ側検査にした理由はソースコメントに残してあります(structured outputs が配列・文字列制約を非サポートのため)。

**層3: done.spec 照合復元**(`app/src/lib/reconcile.ts`)。クライアントはストリームから画面状態を組み立てますが、`done` 受信時にその状態と done.spec を構造照合し(結果はコンソールに記録)、done.spec から表示状態を無条件に再構築します。done.spec はサーバーの最終検証を通過した唯一の正で、正常時は視覚差分ゼロ、破損時だけ静かに自己修復します。

3層に分けたのは、守れる範囲がそれぞれ違うからです。文字列値の内側に収まった重複(「管理理」型)は有効な JSON になるため、層1の構文検査では原理的に検知できません。だから最終防衛を層3に置きました。それでも done.spec 自体が同じ破損を積んでいる場合(転送層の共通モード故障)は救えません。この残る限界は隠さず `partial-json.ts` 冒頭コメントと KNOWN_ISSUES 1-2 に明記しています。

もうひとつ、3層のどれもが受けられない失敗があります。3層+フォールバックはすべて throw・検証違反という「音のする失敗」を待つ設計で、ストリームがエラーも出さずデータも来なくなる沈黙(上流のストール)に対する打ち切りタイマーはありません。失敗を宣言しない失敗は現状拾えない——これは KNOWN_ISSUES 1-5 に切り出しました。

ストリーミング描画には、見落としやすい順序依存もひとつあります。途中経過の `meta` イベントは「appName・icon・description が揃い、かつ fields 配列が現れた時点」で発行しますが、これが安全なのはスキーマの宣言順で `fields` が `description` の直後にあるからです(`"fields":[` が見えた時点で description の文字列が閉じている=切断されていないことが保証される)。プロパティを並べ替えても型もテストも通るのに、描画だけが切れた description を拾いうる。このサイレント回帰は宣言順そのものをテストで凍結して封じました(`appspec.test.ts`)。

## 4. 回転検出 — 不一致は 0 に倒す

FAX にはヘッダだけ正向きで本文が逆さまという紙が実在します(実書類で確認)。向き判定はモデルに本文基準で行わせ、`sharp` で回転します(`claude-live.ts` の `detectRotation`)。50枚検証では 11/50 で発動し、誤発動は 0 でした(この数字は直接 Anthropic 経路での実測です)。

判定は tool_use 強制(`tool_choice`)で、応答スキーマをツール入力に閉じています(2026-08-11 実測: 7/7 でクリーンな tool_use ブロック・全問正解)。以前は JSON 出力をお願いする形で、ルーター経由では散文で返って判定が常に 0 に落ちる=回転補正が実質無効化される不具合がありました。散文パーサ(`app/src/lib/rotation.ts` の `parseRotationResponse`)は移行後も保険として残しています。

もうひとつの規則が合議です。非ゼロ判定が出たときだけ同じ判定をもう一度走らせ、2回が一致したときだけ適用します(`resolveRotationVotes`)。不一致・判定不能は 0(=回さない)に倒します。正向きの画像に 90/180 という誤判定が返るケースを実測しており、誤って回転させると読み取りが崩壊し、捏造のような誤抽出まで誘発するからです。未補正の実害(読み取り精度の低下、ただし信頼度低下→逆質問という受け皿がある)より、誤回転の実害の方がはるかに大きい。だから保守側に倒します。判定に失敗しても元画像で解析を続行するベストエフォート設計です。

合議の限界も書いておきます。2票は同一モデル・同一プロンプト・同一画像なので独立ではありません。一致要求が弾けるのは間欠的な非決定性エラー(KNOWN_ISSUES 3-1 で実測した種類)だけで、プロンプトや画像特性に由来する系統的な誤判定は2票が同じ側に倒れるため合議では弾けません。そこを受ける機構は無く、あるのは50枚で誤発動 0 という実測だけです。

## 5. 入口の防御 — 上限は受信しながら測る

App Router の Route Handler には既定のボディ上限がありません。実測でも 80MB の JSON がそのまま受理されました。未認証の POST でプロセスのメモリを焼ける経路なので、`app/src/lib/http.ts` の `readJsonLimited` で明示的に閉じています。Content-Length の事前判定に加えてストリームを読みながらバイト数を数え、上限を超えた時点で切断します。全量を受け取ってから測るのでは意味がないからです。`/api/analyze` はボディ 12MB・画像 base64 8MB の二段構えで、60MB リクエストが 413 で拒否されることを実測しています。

認証そのものはありません。本構成は会場デモ・ローカル起動の前提で、公開サーバーへの配備は想定外です(その距離の申告は KNOWN_ISSUES 2-4)。

## 6. ルーター互換 — 仕様なら、アプリ側で受ける

ハッカソンの必須要件で、LLM 呼び出しは OrcaRouter 経由です。経路解決は `app/src/lib/llm-client.ts` のファクトリに一元化しました: `LLM_FORCE_DIRECT=1`(障害時の保険)→ `ORCAROUTER_API_KEY` → `ANTHROPIC_API_KEY` → どれも無ければデモのみ。接続自体は `baseURL` の差し替えと Bearer 認証への切り替えだけで、モデル ID はドット表記への決定論変換が要ります(変換関数はテストで固定)。注意点として、`LLM_FORCE_DIRECT=1` かつ Anthropic キー無しの構成は無警告でデモモードに落ちます(意図した挙動としてテストで凍結済み)。

設計判断が要ったのは、透過しないものの扱いです。

**structured outputs は使えない。** 切替直後から応答が ```json フェンス付きで返り、当初は不具合を疑いましたが、公式ドキュメントの対応表で Anthropic 宛の response_format は非対応・tool_use パターン推奨と確認しました(2026-08-10)。仕様なら受け方を変えます。回転判定は公式推奨どおり tool_use 強制へ移行しました。主解析まで tool_use に展開しなかったのは、比較して得るものが小さかったからです。tool_use 化で消えるのはフェンス問題(層1の一部)だけで、ツール入力も `input_json_delta` として細切れに届くため、逐次描画には結局部分JSONパースが要ります。削減幅が小さい割に、50枚検証で実測の安定している主経路を提出直前に差し替えるダウンサイドが大きい——という比較で見送りました。

**明示キャッシュは検算して見送った。** 画像ブロックへの `cache_control` 追加を検討しましたが、実測ではルーター経由のキャッシュヒットはフルプロンプト完全一致時に限られました。画像キャッシュが読まれるのは非ゼロ判定で2回目の合議が走るときだけ——50枚検証で 11/50(約2割)です。入力を 1 に正規化して期待コストを書くと、キャッシュあり: 全リクエストで書き込み割増 1.25 を払い、2割で 0.1 の読み取り → 1.25 + 0.22×0.1 ≒ 1.27。キャッシュなし: 1 + 0.22×1 = 1.22。損益分岐は2回目発生率 約28% で、実測の約2割では回収できません。毎回内容が同一の SYSTEM_PROMPT 側のキャッシュだけ維持しました(`claude-live.ts`)。

usage 報告の異常(input がほぼ 0 になり cache_creation に計上される)も対照実測で追い詰めました。同一リクエスト2連発で write-then-hit を観測し、ルーター側がキャッシュを自動注入していたことが判明、再現スクリプトを渡したところ当日中に修正されています(2026-08-11 実測)。原価推定への実害が出なかったのは、§9 の式が3種のトークンを合算して計上する形だったからです。

## 7. プロンプトの設計判断

スキーマとバリデータの話ばかり書いてきましたが、システムの半分はプロンプトです。本文は `claude-live.ts` の `SYSTEM_PROMPT`(毎回同一の定数で、キャッシュ対象)。判断を4つ抜粋します。

- **保証はコードに置き、プロンプトは補助。** id の命名規則(英小文字スネークケース)はプロンプトとスキーマの description の双方で指示しますが、従われなくても壊れない側に倒します。fields と明細列の id 衝突は決定論リネームで解消し(`appspec.ts` の `resolveColumnIdCollisions`)、なぜ列側をリネームしても安全か(明細行は位置対応・参照は label 照合)という参照解析はコメントに残しました。明細列同士の衝突だけが未対処です(KNOWN_ISSUES 1-3)。
- **確度は正直に申告させ、扱いはコードが決める。**「かすれ・判読困難・業務上の意味の曖昧さがあれば正直に下げる」と confidence の付け方を明示し、その値をどうするか(0.7 未満で逆質問)はコード側の定数です(§8)。
- **旧字体を正規化させない。**「鐵・榮・髙・﨑 などは新字体に置き換えず、そのまま読み取る」。正規化を許すと、誤読が整形された値に化けて幻覚判定(画像に無い値の検出)と区別できなくなるためです。それでも v2 の残欠損2値のうち1件は旧字体の誤読でした。プロンプトで頼んでも消えない種類の誤りとして記録しています。
- **明細は20行で打ち切らせる。** stop_reason が max_tokens の出力は途中で切れた JSON なので、全損として throw します(`claude-live.ts`)。上限に届く前にモデル側で「主要20行+『ほか◯行』」に要約させる方が、切断より多くの情報が残ります。桁区切りと小数点の厳密区別(1,096.100 → 1096.1)の指示も同じ理由で入れました。v1→v2 で明細対応と同時に入れた3変更のうちの1つです。

## 8. 読めなかったときの動線 — 逆質問

帳票読取の価値は読めた項目だけでなく、読めなかった項目を誰がいつ直すかで決まります。読み取り信頼度が 0.7 を下回る項目があると、生成直後のアプリは断定せず「『◯◯』の読み取り信頼度が N% です。この項目をアプリに含めますか?」と操作者に聞きます(`claude-live.ts`)。「いいえ、除外する」でその項目は fields と一覧列から外れ、承認印(stamp)型なら承認フローも一緒に下ろします(`KamiwazaApp.tsx` の `effectiveSpec`)。実書類での実発動も確認しています。

正確に書いておくと、逆質問は最初の低信頼項目1つだけで、複数項目が低信頼のときは残りを聞きません。また、除外した項目を参照する集計カードがライブ経路では残りうる穴を見つけています(KNOWN_ISSUES 1-6)。静かな誤読を人の確認に接続するのがこの動線の役割で、精度の数字はこの受け皿とセットで読んでください。

## 9. 原価推定 — 推定と課金を混ぜない

アプリのヘッダには原価チップがあり、ライブ解析・再構成のたびに推定原価の累計を出します。実装は `app/src/lib/llm-pricing.ts` です。

式は `(input + 0.1×cacheRead + 1.25×cacheCreation) × prompt単価 + output × completion単価`。キャッシュの掛け率はサポートの正式回答とモデルページ公表値で確定した公式レートです(2026-08-11。read 0.1× / write 5分TTL 1.25×。確定前は全種 1× の満額計上=実課金より高く出る方向にしか誤差が出ない保守上限で運用していました)。単価は OrcaRouter `/v1/models` からライブ取得し(TTL 10分・失敗時 60秒のネガティブキャッシュ・解析開始時に事前ウォーム)、取得に失敗したら定数 $5/$25 にフォールバックして UI に「※定数単価」を明示します。この単価取得は throw しない設計です——取得・パースの全異常を吸収して定数単価に落ち、異常系は `llm-pricing.test.ts` の25件で固定してあります。原価表示の失敗で解析ストリームを殺さないためです。

言葉の規律をひとつ決めています。チップの額は usage 実測×公表単価の「推定原価」であり、請求額ではありません。課金の正は OrcaRouter のダッシュボードです(集計レベルの突合は実施済み)。フォールバック単価が1回でも混ざった累計は、全体をフォールバック表記に倒します(`KamiwazaApp.tsx`)。デモ経路では前述のとおり cost 系のキー自体を流さないので、チップは「デモ再生: LLM呼び出しなし」の静的表示になります。キー無しで触る人には実額が見えませんが、実測していない額を画面に出すよりよい、という判断です。なお内訳表示の単価表記に定数固定の食い違いが1つ残っています(KNOWN_ISSUES 1-7)。

## 10. テストは何を守っているか

件数より、何を凍結しているかを4つだけ書きます。

- **「一度目の見え方は1文字も変えていない」の証明。** 提案チップの二度押し欠陥を直したとき、修正前コードで採取した手術ログ・spec構造差分・画面派生値のゴールデントレース(`specdiff.test.ts` の FIRST_APPLY_GOLDEN)と突き合わせ、初回適用の挙動が不変であることを機械で確認しました。ゴールデンのキー集合と実チップ集合の一致検査付きなので、チップを増やすと更新を強制されます。
- **デモの嘘を機械が止める。** `scenarios.test.ts` は、アラートの読み上げ文に埋まった数値(実働時間・支払予定日など、どこにも定数として保存されていない値)をシナリオの実データから再計算して検算します。デモデータを手で直すと文とデータがズレて落ちます。
- **型もテストも通るのに画面だけ壊れる回帰の封じ込め。** スキーマ宣言順への描画の依存(§3)を、宣言順自体のテストで凍結しています(`appspec.test.ts`)。
- **幻覚の構造的封殺を、確率でなく全数で確認。**「日本語で書いて直す」のツールスキーマに注入された実 ID が、全シナリオで必ず reducer に受理されることをプローブします(`specdiff.test.ts`)。enum が守る側と受理する側の集合が食い違えば、そこが幻覚の入り口になるからです。

## 11. 安全工学の語彙で読む設計判断(ISO 26262 / ISO 21448)

> **先に限定します**: 本節は ISO 26262 / ISO 21448(SOTIF)への**準拠主張ではありません**。
> ASIL評価もHARAも車載文脈もありません。ここでやるのは、本アプリの設計判断を
> 両規格の**考え方**で説明し直す対応づけです——概念を借りる理由は、問題構造が同じだからです。

### なぜ帳票読み取りにSOTIFの枠組みか

ISO 26262が扱うのは**故障**の安全(E/E系が壊れたとき)。ISO 21448(SOTIF)が扱うのは
**故障していないのに危険**な場面——センサーやアルゴリズムが仕様どおり動いていても、
性能限界や想定外シナリオで危険が生じるケースです。

**LLMはまさに後者です。** モデルは「故障」しません。仕様どおりに推論して、それでも
誤読し、幻覚を出します。だから本アプリの防御は最初から2系統に分かれています。

### 対応表

| 危険事象 | 種別 | 対策(実装) | 借りた概念 |
|---|---|---|---|
| API障害・SSE切断・JSON破損 | **故障系**(26262的) | 三重保険(フェンス耐性パーサ/スキーマ検証/done.spec照合復元)→ デモリプレイへ自動フォールバック | 故障検出と**安全状態への遷移** |
| フォールバックの誤認 | 故障系 | 「ライブ解析に失敗したため、デモデータで続行します」を画面に明示。以降のデータが撮った紙由来でないと誤認させない | 縮退運転の**運転者への通知** |
| 消費済み原価の誤表示 | 故障系 | ライブ失敗時も消費済みトークンの推定額を表示(「原価$0」と言わない) | 表示の完全性(アナンシエータ) |
| 誤読(仕様どおり動いて間違う) | **性能限界系**(SOTIF的) | 低信頼項目は断定せず**逆質問**として提示→「いいえ」で項目ごと除外 | 性能限界での**人間への引き継ぎ** |
| 幻覚(紙に無い値の生成) | 性能限界系 | 出力をAppSpec DSL(全階層 additionalProperties:false)に閉じ込め、全項目に出典座標を強制。検算行で数値の整合を可視化 | 妥当性監視・検証可能性 |
| 誤回転(180°FAX等の誤判定) | 性能限界系 | 非ゼロ判定のみ**二重合議**、不一致・判定不能は0に倒す——**誤回転の実害の方が未補正の実害より大きい**ため | リスク比較に基づく**フェールセーフ側の選択** |
| 悪意ある入力(紙への指示注入) | 性能限界系 | LLM出力はデータであってコードではない。注入文は「備考欄の値」として取り込まれるのみ(実測: 生成ボタン0件。[SECURITY.md](SECURITY.md)) | 意図機能の誤用シナリオ |
| 未知の危険シナリオ | SOTIFの残余 | 実物スキャン50枚×2周の検証で既知化を進め、**残った限界は [KNOWN_ISSUES.md](KNOWN_ISSUES.md) に列挙**(手持ち撮影未検証・採点器の妥当性未検証 等) | 未知・危険領域の縮小と**残余リスクの開示** |

SOTIFの中心的な営みは「未知の危険シナリオを既知に移し、既知の危険に対策を打ち、
残りを開示して受容判断を仰ぐ」ことです。本リポジトリの 検証(50枚×2周)→対策(上表)→
KNOWN_ISSUES(残余の列挙) という構成は、その循環を紙のワークフローに対して回したものです。

## 12. 主張とコードの対応表

コードリーディングの時間が限られている人向けに、本文書の主張を実装とテストに対応づけておきます。テスト件数は 2026-08-14 実測(`cd app && npm test` = 859件パス+ライブAPI実測用の意図的スキップ1件)。

| 主張 | 実装 | テスト |
|---|---|---|
| フェンス耐性 部分JSONパーサ(第2救済つき) | `app/src/lib/partial-json.ts` | `__tests__/partial-json.test.ts`(27)+ `attack2-repro.test.ts`(23) |
| アプリ側スキーマ検証+意味検査 | `app/src/lib/validate-spec.ts` | `validate-spec.test.ts`(8) |
| done.spec 照合・復元 | `app/src/lib/reconcile.ts` | `reconcile.test.ts`(15) |
| 回転判定のパースと合議規則 | `app/src/lib/rotation.ts` + `claude-live.ts` | `rotation.test.ts`(13) |
| 閉じた6操作と純粋関数 applyDiff | `app/src/lib/specdiff.ts` | `specdiff.test.ts`(270) |
| DSL・ワイヤ変換・id衝突の決定論リネーム | `app/src/lib/appspec.ts` | `appspec.test.ts`(108) |
| デモ/ライブ同一イベント列 | `app/src/lib/events.ts` + `demo.ts` | `scenarios.test.ts`(295)が整合を凍結 |
| 経路解決(OrcaRouter/直接/デモ) | `app/src/lib/llm-client.ts` | `llm-client.test.ts`(21) |
| 原価推定(公式レート・フォールバック) | `app/src/lib/llm-pricing.ts` | `llm-pricing.test.ts`(25) |
| 出典ハイライトのタッチ規則 | `app/src/lib/highlight.ts` | `highlight.test.ts`(18) |
| リクエストボディ12MB上限 | `app/src/lib/http.ts` | —(実測: 60MBリクエストを413で拒否) |

テストがあるからバグは無い、とは言いません。テストが不具合を6件あぶり出し、直して回帰テストで固定した——言えるのはそこまでです(発見と修正の経緯は解説記事に公開。残っている限定は [KNOWN_ISSUES.md](KNOWN_ISSUES.md))。
