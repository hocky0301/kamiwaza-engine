# 既知の問題と限界(KNOWN ISSUES)

見つけて直した不具合の記録は内部の主張台帳(全主張に根拠と限定句を紐づけた検証記録。要点は解説記事に公開)にあります。この文書はその逆で、**まだ残っているもの**を先に言うための場所です。審査や質疑で指摘される前に、私から書いておきます。各項目は 症状 / 影響範囲 / 現在の緩和策 / 直すならどうするか の順です。

設計判断の背景は [ARCHITECTURE.md](ARCHITECTURE.md) を参照してください。

---

## 1. コードに残る既知の限定

### 1-1. `addField` の label だけ trim されない(F04 の残り)

- **症状**: 自由文・チップ経由で項目を追加するとき、`id` は trim して保存するのに `label` はバリデーションだけ trim 後で行い、保存は生値のまま。前後に空白のある label がそのまま画面に載ります。
- **影響範囲**: 「日本語で書いて直す」の `addField` 経路のみ。解析経路には関係ありません。
- **緩和策**: 非対称であること自体をテストで明示的に固定してあります(`specdiff.test.ts` の「【既知の非対称】label は trim されない」)。挙動が黙って変わることはありません。
- **直すなら**: 保存時に label も trim します。ただし `renameField` などの検索・手術ログ・エラー文言が生値前提で揃っているため、片側だけ直すと今度はログと実体がズレます。関係箇所を同時に揃える必要があり、提出直前に触る変更ではないと判断して残しました。

### 1-2. 文字列値の内側の重複は構文検査で検知できない(F06 の原理的限界)

- **症状**: 転送層でデルタが二重適用されたとき、重複が JSON の文字列値の内側に収まると(「出荷指示管理」→「出荷指示管理理」)、結果は有効な JSON のままです。構文検査では原理的に検知できません。
- **影響範囲**: ライブ解析のストリーミング表示。さらに SDK の最終メッセージも同一 SSE 列から蓄積するため、転送層の重複は done.spec にも同相で乗りえます(共通モード故障)。
- **緩和策**: 末尾の構造文字重複は部分パーサの第2救済で復元し(`partial-json.ts`)、クライアント組み立ての破損は done 受信時の照合・再構築(`reconcile.ts`)が受けます。done.spec 自体が破損している場合は救えませんが、その場合も画面と spec の整合は保たれます。
- **直すなら**: アプリ側では閉じません。転送路にシーケンス番号やチェックサムを足して重複そのものを検出する必要があり、SSE と SDK の中間層に手を入れる話になります。限界の所在は `partial-json.ts` 冒頭コメントに明記しました。

### 1-3. 明細列同士の id 重複は未対処(F03 の残り)

- **症状**: 解析結果の fields と明細列の id 衝突は決定論リネームで解消しますが(`appspec.ts` の `resolveColumnIdCollisions`)、明細列同士だけが同じ id を持つケースは対象外です。後の列が先の列の値を上書きします。
- **影響範囲**: ライブ解析でモデルが列 id を重複させた場合のみ。デモ5シナリオでは起きません(テストで整合を固定済み)。
- **緩和策**: プロンプトで列 id の命名規則を指示しており、50枚検証・会期中のライブ実走で発現していません。
- **直すなら**: 同じ関数に列同士の衝突検出を足すだけで、実装は小さい。優先度の問題で fields×列の衝突(実害の導線が多い方)を先に塞ぎました。

### 1-4. キー無し時の自由文解釈は語彙が狭い

- **症状**: APIキー無しの構成では自由文をキーワードフォールバック(`specdiff.ts` の `keywordFallback`)で解釈しますが、正規表現の語彙が狭い。具体的には、READMEの看板コピーそのままの「8万円超の発注は社長承認に」が変換失敗になります。数量ゲートが「超え」を要求して「超」に一致せず、「承認に」も承認系のトリガー語(追加/段階/入れ 等)を含まないためです。
- **影響範囲**: キー無し構成の自由文入力のみ。キーがあれば LLM の tool use が解釈します。提案チップは決定論適用なので、同じ内容の変更はキー無しでも確実に動きます。
- **緩和策**: チップに誘導する UI 構成。言い換え(「承認を追加して、合計金額が8万円以上はアラート」)なら通ることを確認済みです。
- **直すなら**: トリガーに `超(え|過)?` と `承認(に|へ|を|フロー)` を足します。フォールバックはあくまで縮退運転であり、語彙を育てるより「どこまで動けば十分か」を決める方が先だと考えています。

---

## 2. 実装しないという判断が、同時に制約でもあるもの

### 2-1. 保存機能が無い(再読込で消える)

- **症状**: 生成したアプリと入力データはブラウザのメモリ上だけにあり、再読込で消えます。
- **影響範囲**: 全経路。継続利用はできません。
- **緩和策**: これは半分は設計選択です。本番コードにファイル書き込みの経路は0件、ブラウザ側も localStorage / sessionStorage / indexedDB は全域0件(いずれも grep 実測)。つまり紙の画像も読み取った値も、自サーバのディスクには書かれず、置き忘れる場所がそもそもありません。CSV書き出しで手元に残せます。
- **直すなら**: spec と records の永続化はスキーマが閉じているぶん素直です(AppSpec は JSON そのもの)。ただし保存した瞬間に保持ポリシー・暗号化・削除の設計が必要になり、「預かるならちゃんと預かる」を作り込むのは製品化フェーズの仕事だと線を引きました。

### 2-2. 自動マスキングは未実装

- **症状**: 撮影した画像に実在の社名・氏名が写っていても、送信前に自動でマスクする処理はありません。画像はそのまま LLM 経路(OrcaRouter → モデル提供元)へ渡ります。
- **影響範囲**: ライブ解析の全リクエスト。
- **緩和策**: 運用で守っています。第三者(来場者・審査員)の紙は受け取らない、読むのは架空情報の体験用紙・自作の合成帳票だけ、撮る前に自分の目で1度確認する。ルーター側の PII 検査(PII Shield)に頼れないことも確認済みです——組み込みの検出対象はクローズドリストで氏名・住所・社名を含まず(公式ドキュメント)、私のワークスペースでは未設定であることを実測で確認しました(2026-08-11。架空の電話番号を含む画像・テキストとも素通り)。下流の保持については、OrcaRouter 区間は公式ドキュメントに「ディスクに書き込まない・学習に使わない」と明記があります(公式ドキュメントの記載であって、私が検証した事実ではありません)。モデル提供元区間は各社のポリシーの範囲で、私からは断定しません。
- **直すなら**: 送信前のローカル前処理として、検出(座標つき)→画像側をぼかしてから上流へ、という2段になります。手書き帳票の氏名検出は OCR+固有表現抽出の精度がそのまま安全性になるため、「未実装のまま運用で守る」と「中途半端に実装して守れた気になる」の二択なら前者を選びました。

### 2-3. 承認フローは「設計」まで

- **症状**: 生成されるのは承認ステップの定義(名前とロール)だけで、承認・差戻しのアクションや状態遷移はありません。
- **影響範囲**: 全シナリオ・全経路。画面の承認フロー欄は表示のみです。
- **緩和策**: 言い方を統一しています。「承認フローを自動設計する」までが事実で、「承認が回る」とは言いません。
- **直すなら**: 状態を持った瞬間に永続化(2-1)と通知・権限の設計が芋づるで必要になります。実行基盤は既存のワークフロー製品に委ね、カミワザは紙から設計を起こす側に集中する、という切り分けを意図して選びました。

---

## 3. 上流・検証手法に由来する限界

### 3-1. vision の間欠(上流由来)

- **症状**: 画像つきリクエストで、画像を見ていないとしか思えない応答が返る日がありました。回転判定では正向きの画像に 90/180 という誤回転値が返るケースを実測しています。
- **影響範囲**: ライブ経路のみ。頻度は低く、常時再現もしません。上流(ルーター/モデル)由来のため、根絶は私のコードでは達成できません。
- **緩和策**: 誤回転は二重確認・合議一致時のみ適用(不一致は回さない側に倒す)、判定は tool_use 強制でスキーマに閉じ、失敗しても元画像で解析続行。本解析側は読み取り信頼度の低下が逆質問として表面化する設計で、静かな誤読を人の確認に接続します。防御であって根絶ではない、というのが正確な言い方です。
- **直すなら**: リトライ規則の追加、応答に画像内容の要約を含めさせて突き合わせる自己検証、経路の多重化などが候補です。いずれも間欠事象の再現データを増やしてからでないと、効いたかどうか判定できません。

### 3-2. 数値はモデル・経路に依存する(持ち越せない)

- **症状**: 精度の数値(96%・99.87%・幻覚0件)は特定モデル・直接 Anthropic 経路での実測です。OrcaRouter 経由では応答形式から挙動が変わることを実測しており(structured outputs 非適用など)、モデルや経路が変われば数字は持ち越せません。
- **影響範囲**: 公開しているすべての精度数値の解釈。
- **緩和策**: だからすべての数値に経路と日付を付けています。ルーター経路そのものは E2E で通しました(2026-08-11 実測: 正向きの紙で 47.0秒、180°逆さまFAXで検出→自動補正→抽出まで 38.2秒)。回転補正の「誤発動0」も 50枚検証(直接経路)という限定つきで言います。
- **直すなら**: 経路・モデルごとの再検証バッチを回すこと。原価側のハーネス(`tools/cost-batch`)は用意済みですが、精度側は元画像セットが失われているため(3-3)、新しい検証セットの構築からになります。

### 3-3. 検証は LLM 採点前提で、同一セットの再実行ができない

- **症状**: 50枚バッチの採点は、抽出 JSON と原本画像を照合する採点エージェント(約50体)によるもので、LLM 採点です。また元画像 PNG を保存しておらず、同一セットでの再実行はできません。
- **影響範囲**: 全項目正解 96%(48/50)・値レベル 99.87%(1,516/1,518)・幻覚 0/2,225 の主要数値すべて。
- **緩和策**: 採点基準はスクリプト固定で v1/v2 同一、ルーブリックと採点生データは提示可能です(git 管理外・要請あれば開示)。統計的な言い方も縛っています——76%→96% は 95%信頼区間が重なるため「有意に改善」とは言わず、主力は n=1,518 で区間の狭い値レベル 99.87% に置いています。v1→v2 の変更が3変数同時であること、success→partial の退行が1件あることも開示済みです。
- **直すなら**: 保持ポリシー込みで検証セットを再構築し、人手のサンプル採点を混ぜて LLM 採点との一致率を測ります。「LLM採点である」という留保自体を数値で小さくしていく方向です。

### 3-4. 出典ハイライトのライブ座標精度は未検証

- **症状**: デモ5シナリオの sourceBox は手作業で付与した座標なのでズレません。ライブ解析もモデルが sourceBox を返しますが、実写真上でどの程度正確かは定量検証していません。
- **影響範囲**: ライブ経路の出典ハイライトの位置精度。
- **緩和策**: 座標系は紙の描画と同じ%座標に統一してあり、構造的なズレ(座標系の食い違い)は起きません。残るのはモデルの推定精度だけです。
- **直すなら**: 正解ボックスを付けた検証セットで重なり(IoU)を測るだけなので、検証設計は単純です。手が回っていないだけ、と正直に書いておきます。

### 3-5. デモ5シナリオのうち2種は実書類コーパスに実在しない

- **症状**: 「実在の紙が仕様を決める」と言っていますが、デモの作業日報・設備点検表の2シナリオは、分析した26年分コーパスに同種の紙がありません(想定シナリオです)。
- **影響範囲**: デモの5シナリオ中2つの「実在性」の主張。読み取り精度の検証(実スキャン50枚)には含まれていないので、精度数値には影響しません。
- **緩和策**: この2本については実在の紙由来だと言わない、という言い方の規律で対処しています。
- **直すなら**: 業種の異なる協力先から同種の実帳票を借りて置き換えるのが正道です。これは製品検証としてもそのまま次の一歩になります。
