# 既知の問題と限界(KNOWN ISSUES)

見つけて直した不具合の記録は内部の主張台帳(全主張に根拠と限定句を紐づけた検証記録。要点は解説記事に公開)にあります。この文書はその逆で、**まだ残っているもの**を先に言うための場所です。審査や質疑で指摘される前に、私から書いておきます。各項目は 症状 / 影響範囲 / 現在の緩和策 / 直すならどうするか の順です。

設計判断の背景は [ARCHITECTURE.md](ARCHITECTURE.md) を参照してください。

---

## 1. コードに残る既知の限定

### 1-1. `addField` の label だけ trim されない

- **症状**: 自由文・チップ経由で項目を追加するとき、`id` は trim して保存するのに `label` はバリデーションだけ trim 後で行い、保存は生値のまま。前後に空白のある label がそのまま画面に載ります。
- **影響範囲**: 「日本語で書いて直す」の `addField` 経路のみ。解析経路には関係ありません。
- **緩和策**: 非対称であること自体をテストで明示的に固定してあります(`specdiff.test.ts` の「【既知の非対称】label は trim されない」)。挙動が黙って変わることはありません。
- **直すなら**: 保存時に label も trim します。ただし `renameField` などの検索・手術ログ・エラー文言が生値前提で揃っているため、片側だけ直すと今度はログと実体がズレます。関係箇所を同時に揃える必要があり、提出直前に触る変更ではないと判断して残しました。

### 1-2. 文字列値の内側の重複は構文検査で検知できない(原理的限界)

- **症状**: 転送層でデルタが二重適用されたとき、重複が JSON の文字列値の内側に収まると(「出荷指示管理」→「出荷指示管理理」)、結果は有効な JSON のままです。構文検査では原理的に検知できません。
- **影響範囲**: ライブ解析のストリーミング表示。さらに SDK の最終メッセージも同一 SSE 列から蓄積するため、転送層の重複は done.spec にも同相で乗りえます(共通モード故障)。
- **緩和策**: 末尾の構造文字重複は部分パーサの第2救済で復元し(`partial-json.ts`)、クライアント組み立ての破損は done 受信時の照合・再構築(`reconcile.ts`)が受けます。done.spec 自体が破損している場合は救えませんが、その場合も画面と spec の整合は保たれます。
- **直すなら**: アプリ側では閉じません。転送路にシーケンス番号やチェックサムを足して重複そのものを検出する必要があり、SSE と SDK の中間層に手を入れる話になります。限界の所在は `partial-json.ts` 冒頭コメントに明記しました。

### 1-3. 明細列同士の id 重複は未対処

- **症状**: 解析結果の fields と明細列の id 衝突は決定論リネームで解消しますが(`appspec.ts` の `resolveColumnIdCollisions`)、明細列同士だけが同じ id を持つケースは対象外です。後の列が先の列の値を上書きします。
- **影響範囲**: ライブ解析でモデルが列 id を重複させた場合のみ。デモ5シナリオでは起きません(テストで整合を固定済み)。
- **緩和策**: 列 id の命名規則はプロンプトとスキーマの description で指示しており(保証ではなく緩和)、50枚検証・会期中のライブ実走で発現していません。
- **直すなら**: 同じ関数に列同士の衝突検出を足すだけで、実装は小さい。優先度の問題で fields×列の衝突(実害の導線が多い方)を先に塞ぎました。

### 1-4. キー無し時の自由文解釈は語彙が狭い

- **症状**: APIキー無しの構成では自由文をキーワードフォールバック(`specdiff.ts` の `keywordFallback`)で解釈しますが、正規表現の語彙が狭い。具体的には、READMEの看板コピーそのままの「8万円超の発注は社長承認に」が変換失敗になります。数量ゲートが「超え」を要求して「超」に一致せず、「承認に」も承認系のトリガー語(追加/段階/入れ 等)を含まないためです。
- **影響範囲**: キー無し構成の自由文入力のみ。キーがあれば LLM の tool use が解釈します。提案チップは決定論適用なので、同じ内容の変更はキー無しでも確実に動きます。
- **緩和策**: チップに誘導する UI 構成。言い換え(「承認を追加して、合計金額が8万円以上はアラート」)なら通ることを確認済みです。
- **直すなら**: トリガーに `超(え|過)?` と `承認(に|へ|を|フロー)` を足します。フォールバックはあくまで縮退運転であり、語彙を育てるより「どこまで動けば十分か」を決める方が先だと考えています。

### 1-5. ストリームの「沈黙」に対する打ち切りが無い

- **症状**: ライブ解析中に上流ストリームがエラーも出さずデータも寄こさなくなった場合(ストール・中間経路の滞留)、打ち切るタイマーがありません(`claude-live.ts` にウォッチドッグ・タイムアウトは未実装)。クライアントは待ち続けます。
- **影響範囲**: ライブ経路のみ。デモ経路は決定論のローカル再生なので沈黙しません。
- **緩和策**: 実質ありません。3層防御もデモフォールバックも throw・検証違反という「音のする失敗」を受ける設計で、コネクション断や HTTP エラーはそこで表面化しますが、「接続は生きているがデータが来ない」だけは現状どの層も拾えません。失敗を宣言しない失敗への備えが抜けている、というのが正確な自己評価です。
- **直すなら**: 最終イベントからの経過時間で打ち切る監視タイマーを1本入れ、既存のデモフォールバック連鎖に接続します。受け皿は実装済みなので、足すのは打ち切りの判定だけです。

### 1-6. 逆質問で除外した項目が、集計カードに残りうる

- **症状**: 逆質問に「いいえ、除外する」と答えると fields と一覧列から項目を除外しますが(`KamiwazaApp.tsx` の `effectiveSpec`)、aggregations は掃除しません。ライブ解析でモデルが低信頼項目を集計対象に選んでいた場合、除外後もその集計カードが残り、値も表示され続けます(firstRecord にキーが残るため)。
- **影響範囲**: ライブ経路のみ。デモ5シナリオでは逆質問対象を参照する集計がありません。正確に書くと、この不交差を直接検査するテストは無く、`scenarios.test.ts` が凍結しているのは集計の顔ぶれ・値と逆質問の対象までです。ただし sum/avg には対象が number 型限定という検査があり、逆質問の対象は4件とも非 number 型(stamp 3・select 1)なので、テストを通ったまま交差しうるのは count の fieldId だけ——そして描画は count で fieldId を読みません(`SpecApp.tsx`)。
- **緩和策**: 発現には「低信頼と申告された項目を、モデル自身が集計対象に選ぶ」という条件が要ります。ライブ実走では未観測ですが、観測していないだけで、構造的には塞がっていません。
- **直すなら**: `effectiveSpec` で aggregations も fieldId 参照で除外する数行の修正です。提出直前に画面挙動を変えないため、直すより先に記録しておくことを選びました。

### 1-7. 原価チップの内訳行が単価を定数表示している

- **症状**: 原価チップの詳細行が単価を「$5/$25 per 1Mtok」という定数文字列で表示します(`SpecApp.tsx` の `costDetailLines`)。単価をライブ取得できた場合に実測単価が定数と異なると、合計額は実測単価で正しいのに、内訳行の単価表記だけが食い違います。
- **影響範囲**: 原価チップの詳細表示のみ。合計・累計の額は正しい。
- **緩和策**: 実測時点ではライブ取得単価が定数 $5/$25 と一致しており、食い違いは顕在化していません(2026-08-09 実測: 表示額と手計算の完全一致を確認)。
- **直すなら**: 表示側の状態(CostState)が単価を持っていないため、表示だけでは直せません。単価を state に載せて内訳行に流す小改修です。「誠実表示」を売りにしている部分での不整合なので、優先度は見た目より高いと思っています。

---

## 2. 実装しないという判断が、同時に制約でもあるもの

### 2-1. 保存機能が無い(再読込で消える)

- **症状**: 生成したアプリと入力データはブラウザのメモリ上だけにあり、再読込で消えます。
- **影響範囲**: 全経路。継続利用はできません。
- **緩和策**: これは半分は設計選択です。紙の画像・読み取った値をディスクやブラウザストレージに書く経路は0件(ファイル書き込みは監査ログ=ハッシュとメタデータのみの1経路、localStorageは効果音設定フラグ1件のみ)。つまり業務データを置き忘れる場所がそもそもありません。CSV/ZIP書き出しで手元に残せます。
- **直すなら**: spec と records の永続化はスキーマが閉じているぶん素直です(AppSpec は JSON そのもの)。ただし保存した瞬間に保持ポリシー・暗号化・削除の設計が必要になり、「預かるならちゃんと預かる」を作り込むのは製品化フェーズの仕事だと線を引きました。

- **2026-08-14追記**: 業務データ本体は引き続き保存しないが、**監査ログ(JSONL)は追加した**——
  逆質問への回答・「日本語で書いて直す」の差分・解析ごとのトークン/原価/検証結果を、
  ハッシュとメタデータのみで記録する(`var/audit.jsonl`)。「モデルの出力を人間がどう直したか」の
  収集が目的で、記入値の保存機能ではない。

### 2-2. 自動マスキングは未実装

- **症状**: 撮影した画像に実在の社名・氏名が写っていても、送信前に自動でマスクする処理はありません。画像はそのまま LLM 経路(OrcaRouter → モデル提供元)へ渡ります。
- **影響範囲**: ライブ解析の全リクエスト。
- **緩和策**: 運用で守っています。第三者(来場者・審査員)の紙は受け取らない、読むのは架空情報の体験用紙・自作の合成帳票・注入検証用帳票の3種だけ、撮る前に自分の目で1度確認する。ルーター側の PII 検査(PII Shield)に頼れないことも確認済みです——組み込みの検出対象はクローズドリストで氏名・住所・社名を含まず(公式ドキュメント)、私のワークスペースでは未設定であることを実測で確認しました(2026-08-11。架空の電話番号を含む画像・テキストとも素通り)。下流の保持については、OrcaRouter 区間は公式ドキュメントに「ディスクに書き込まない・学習に使わない」と明記があります(公式ドキュメントの記載であって、私が検証した事実ではありません)。モデル提供元区間は各社のポリシーの範囲で、私からは断定しません。
- **直すなら**: 送信前のローカル前処理として、検出(座標つき)→画像側をぼかしてから上流へ、という2段になります。手書き帳票の氏名検出は OCR+固有表現抽出の精度がそのまま安全性になるため、「未実装のまま運用で守る」と「中途半端に実装して守れた気になる」の二択なら前者を選びました。

### 2-3. 承認フローは「設計」まで

- **症状**: 生成されるのは承認ステップの定義(名前とロール)だけで、承認・差戻しのアクションや状態遷移はありません。
- **影響範囲**: 全シナリオ・全経路。画面の承認フロー欄は表示のみです。
- **緩和策**: 言い方を統一しています。「承認フローを自動設計する」までが事実で、「承認が回る」とは言いません。
- **直すなら**: 状態を持った瞬間に永続化(2-1)と通知・権限の設計が芋づるで必要になります。実行基盤は既存のワークフロー製品に委ね、カミワザは紙から設計を起こす側に集中する、という切り分けを意図して選びました。

### 2-4. 単一利用者・会場デモ配備の前提(認証なし・同時実行未検証)

- **症状**: `/api/analyze`・`/api/reconfigure` に認証はありません。公開サーバーに配備すると、第三者が作者の API キーで推論を回せます。また同時に複数の解析ストリームが走る構成での挙動(ボディ12MB×N のメモリ・SSE の多重)は検証していません。
- **影響範囲**: 配備形態の選択。**「ローカルだから安全」ではありません**——会場デモは
  iPad から同一LAN越しに `http://<PCのIP>:3000` を叩く構成なので、その場のネットワークに
  いる第三者からは到達可能です(会場Wi-Fiではなく自前テザリングを使うのはこのためですが、
  テザリングに接続できる相手には届きます)。
- **緩和策**: 3段で資源側を絞っています。①ボディ上限(analyze 12MB / reconfigure 256KB・`http.ts`)
  ②`/api/reconfigure` の形状ゲート(fields≤80 / columns≤40 / ラベル≤120字)——specBrief と
  ツール定義が項目数に比例してプロンプト長を押し上げるため、バイト数だけでは止まりません
  ③自由文の上限2,000字。**いずれも資源保護であって認証の代替ではありません**。
- **直すなら**: 公開配備するなら認証とレート制限が先です。検証していない構成を「動く」とは言いません。

---

## 3. 上流・検証手法に由来する限界

### 3-1. vision の間欠(上流由来)

- **症状**: 画像つきリクエストで、画像を見ていないとしか思えない応答が返る日がありました。回転判定では正向きの画像に 90/180 という誤回転値が返るケースを実測しています。
- **影響範囲**: ライブ経路のみ。頻度は低く、常時再現もしません。上流(ルーター/モデル)由来のため、根絶は私のコードでは達成できません。
- **緩和策**: 誤回転は二重確認・合議一致時のみ適用(不一致は回さない側に倒す)、判定は tool_use 強制でスキーマに閉じ、失敗しても元画像で解析続行。本解析側は読み取り信頼度の低下が逆質問として表面化する設計で、静かな誤読を人の確認に接続します。防御であって根絶ではない、というのが正確な言い方です。
- **直すなら**: リトライ規則の追加、応答に画像内容の要約を含めさせて突き合わせる自己検証、経路の多重化などが候補です。いずれも間欠事象の再現データを増やしてからでないと、効いたかどうか判定できません。

### 3-2. 数値はモデル・経路に依存する(持ち越せない)

- **症状**: 精度の数値(96%・99.87%・捏造0件)は特定モデル・直接 Anthropic 経路での実測です。OrcaRouter 経由では応答形式から挙動が変わることを実測しており(structured outputs 非適用など)、モデルや経路が変われば数字は持ち越せません。
- **影響範囲**: 公開しているすべての精度数値の解釈。
- **緩和策**: だからすべての数値に経路と日付を付けています。ルーター経路そのものは E2E で通しました(2026-08-11 実測: 正向きの紙で 47.0秒、180°逆さまFAXで検出→自動補正→抽出まで 38.2秒)。回転補正の「誤発動0」も 50枚検証(直接経路)という限定つきで言います。
- **直すなら**: 経路・モデルごとの再検証バッチを回すこと。原価側のハーネス(`tools/cost-batch`)は用意済みですが、精度側は元画像セットが失われているため(3-3)、新しい検証セットの構築からになります。

### 3-3. 検証は LLM 採点で、同一セットの再実行ができない

- **症状**: 50枚バッチの採点は、抽出 JSON と原本画像を照合する採点エージェント(約50体)によるもので、LLM 採点です。採点器自体の妥当性検証(人手採点との一致率・見逃し率の推定)は行っていません。したがって「捏造0件」の正確な読みは「採点器が検出した捏造が0件」であり、採点器の見逃し率に依存します。また元画像 PNG を保存しておらず、同一セットでの再実行はできません。
- **影響範囲**: 全項目正解 96%(48/50)・値レベル 99.87%(1,516/1,518)・捏造 0/2,225 の主要数値すべて。
- **緩和策**: 採点対象(抽出結果)と採点根拠(原本画像)が独立しているため自己採点ではなく、採点基準はスクリプト固定で v1/v2 同一です。ルーブリックと採点生データは提示可能です(git 管理外・要請あれば開示)。50枚の抽出は帳票5類型(手書き見積FAX・貿易定型帳票・印字納品FAX・複写伝票スキャン・様式多様な仕入請求書)×各10枚のシード固定サンプリングで、都合の良い紙だけを選んだものではありません。統計的な言い方も縛っています——v1→v2(38/50→48/50)は同一50枚の対応あり比較なので、検定するなら McNemar 検定が筋ですが、変更が3変数同時で単一要因の統制になっておらず、success→partial の退行も1件あるため、有意性の主張自体をしません。値レベルの n=1,518 も帳票内で値が相関するため(1枚読み損ねると数十値が同時に落ちる)、独立試行とみなした信頼区間は実際より狭く出ます。だから帳票単位の 48/50 を必ず併記します。
- **直すなら**: 保持ポリシー込みで検証セットを再構築し、人手のサンプル採点を混ぜて LLM 採点との一致率を測ります。「LLM採点である」という留保自体を数値で小さくしていく方向です。

### 3-4. 出典ハイライトのライブ座標精度は未検証

- **症状**: デモ5シナリオの sourceBox は手作業で付与した座標なのでズレません。ライブ解析もモデルが sourceBox を返しますが、実写真上でどの程度正確かは定量検証していません。
- **影響範囲**: ライブ経路の出典ハイライトの位置精度。
- **緩和策**: 座標系は紙の描画と同じ%座標に統一してあり、構造的なズレ(座標系の食い違い)は起きません。残るのはモデルの推定精度だけです。
- **直すなら**: 正解ボックスを付けた検証セットで重なり(IoU)を測るだけなので、検証設計は単純です。手が回っていないだけ、と正直に書いておきます。

### 3-5. デモ5シナリオのうち2種は実書類コーパスに実在しない

- **症状**: 「実在の紙が仕様を決める」と言っていますが、デモの作業日報・設備点検表の2シナリオは、分析した26年分コーパスに同種の紙がありません(想定シナリオです)。
- **影響範囲**: デモの5シナリオ中2つの「実在性」の主張。読み取り精度の検証(実スキャン50枚)には含まれていないので、精度数値には影響しません。
- **緩和策**: この2本については実在の紙由来だと言わない、という言い方の規律で対処しています。
- **直すなら**: 業種の異なる協力先から同種の実帳票を借りて置き換えるのが正道です。これは製品検証としてもそのまま次の一歩になります。

### 3-6. 検証はスキャン画像で、看板は「写真1枚」

- **症状**: 精度検証の50枚は実スキャン画像です。一方、製品の一行目は「写真1枚」。倉庫の照明の下で片手で撮った、影・台形歪み・ブレのあるスマホ写真で同じ数字が出る根拠はまだありません。
- **影響範囲**: README 検証実績表の全数値の適用範囲。ライブ経路の実走は E2E 通過の確認までです(3-2)。
- **緩和策**: 撮影品質の劣化は読み取り信頼度の低下→逆質問として表面化する設計ですが、その連動自体も撮影画像では定量検証していません。設計であって実証ではない、と書いておきます。
- **直すなら**: 同一帳票をスキャンと手持ち撮影の両方で読ませる対照セットを作ります。差分がそのまま「撮影条件の頑健性」の実測になります。

### 3-7. 検証コーパスは1社由来(レイアウト分布の偏り)

- **症状**: 26年分・14,608件のコーパスは実在企業1社のアーカイブで、帳票レイアウトの分布はその1社の取引先に閉じています。同じ「注文書」でも発行元が変われば別のレイアウト・別の手書きの癖・別の複写式になるのが帳票の現実で、50枚検証も同じコーパス由来である以上、96% は「この1社の紙の生態系での96%」です。
- **影響範囲**: 検証実績の一般化可能性。時間方向(26年)には深く、発行元方向には狭い、という非対称です。
- **緩和策**: 50枚は類型を跨いだサンプリング(3-3)で、単一様式への過適合は避けています。ただし類型の多様性と発行元の多様性は別物です。
- **直すなら**: 業種の異なる協力先の実帳票で検証セットを再構築します(3-5 の置き換えと同じ次の一歩です)。他社の紙で同じ数値が出る根拠は、まだ無い——と先に言っておきます。
